appname: agarage
version: 1

handle:

#URL重写
- rewrite: if(!-f && !-d && %{REQUEST_URI} ~ "^(.*)\.css$") goto "/site/web/%1.css"
- rewrite: if(!-f && !-d && %{REQUEST_URI} ~* "(.*)") goto "/site/web/app_dev_sae.php/%1"